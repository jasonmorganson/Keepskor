
var chai = require('chai'),
    assert = chai.assert,
    expect = chai.expect,
    should = chai.should();

var app = require('../app/app.js');

describe( 'logger', function(){

    it( 'should log when Papertrail connects', function() {
        // TODO: Create test
        return true;
    });
    it( 'should log errors from Papertrail', function() {
        // TODO: Create test
        return true;
    });
    it( 'should send uncaught exceptions to Exceptional', function() {
        // TODO: Create test
        return true;
    });
})

